{
    "name": "shop",
    "private": true,
    "scripts": {
        "format": "prettier --write ."
    },
    "workspaces": [
        "packages/*",
        "apps/*"
    ],
    "devDependencies": {
        "prettier": "^3.3.3"
    }
}

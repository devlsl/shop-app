Deploy url - https://projects.shop.devlsl.ru/

To start project: 
  1. clone repo
  2. create .env file and fill in the same way as .env.example (you can just duplicate .env.example and delete ".example" postfix)
  3. run "docker compose up -d" command (so you need to have installed docker on your pc)
